const Install = () => {
    return (
        <div>
            <h3>Follow the link to install the extension</h3>
            <a href="https://metamask.io/download/">MetaMask</a>
        </div>
    );
}

export default Install ;